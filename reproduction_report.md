# Reproduction report

**Verdict: reproduces.** Every command in `README.md` was run from a clean export of this tree
in a fresh virtual environment. Every command exited with status 0. Of the 16 versioned CSV
files, 15 were regenerated byte-identical, and the 16th agrees in every cell except the
wall-clock fields.

Date: 2026-09-19, 13:14–13:46 (UTC−3).

## What was tested

- **Tree.** The staged git tree `a3254a4bbb9962876cb6314206ab467482b5f59e` (74 files), exported
  with `git checkout-index` into an empty directory under `/tmp`. The export has no `.git`, so
  the runs log `code_commit: git:unknown`. From a clone they would log the commit. What changed
  after the test is documentation only and enters no computation: the added `README.md`,
  `CITATION.cff`, `LICENSE`, `LICENSE-DATA` and this report, and the runtime comment on the
  `fixed_pair.py` line of `experiments/2026-09-18_binary_tree_family/README.md` (≈ 5 → ≈ 1
  min, as measured below).
- **Environment.** `python3 -m venv` plus `pip install -r requirements.txt` from PyPI, with
  nothing preinstalled. The resulting `pip freeze`:
  `contourpy 1.4.0, cycler 0.12.1, fonttools 4.65.0, kiwisolver 1.5.1, matplotlib 3.11.2,
  mpmath 1.3.0, numpy 2.5.3, packaging 26.3, pillow 12.3.0, pyparsing 3.3.2,
  python-dateutil 2.9.0.post0, python-flint 0.9.0, SciencePlots 2.2.2, scipy 1.18.1,
  six 1.17.0`.
- **Machine.** The same machine that produced the results: AMD Ryzen 7 3700U, 8 threads,
  9.6 GB RAM, Linux 7.0.0-31, Python 3.12.3, TeX Live 2023 for the figure. This checks the
  tree and the dependency list. It does not check portability to another OS, CPU or Python
  version.
- **Order.** `interval_certification` ran first. After it, four chains ran in parallel:
  `mode_localization`, `exact_benchmarks`, `binary_tree_family`, and `verification/`.

## Commands and wall time

| Step | Command (from `experiments/<id>/code/` or `verification/`) | Exit | Wall time |
|---|---|---|---|
| certification, 256 bits | `python run.py --prec 256` | 0 | 83 s |
| certification, 512 bits | `python run.py --prec 512` | 0 | 95 s |
| cross-check | `python crosscheck.py` | 0 | 15 s |
| mass–spring | `python run.py --kmax 10` | 0 | 69 s |
| exact benchmarks | `python run_benchmark.py` | 0 | 822 s |
| figure sweep | `python run_sweep.py` | 0 | 143 s |
| decay fits | `python analysis.py` | 0 | < 1 s |
| figure | `python make_figure.py` | 0 | 22 s |
| binary tree | `python run.py --rmax 12 --rexact 5` | 0 | 1661 s |
| binary tree, fixed pair | `python fixed_pair.py` | 0 | 35 s |
| re-derivation of Examples 7.1–7.3 | `python verify_examples.py` | 0 | 22 s |
| Proposition 8.1 | `python verify_prop_comparacao_round3.py` | 0 | 14 s |

Total 31.5 min with the parallel phase. The self-checks printed by the scripts:
- `crosscheck: ok`;
- `OVERALL: ALL EXAMPLE CLAIMS REPRODUCED`;
- `NO VIOLATION`;
- `checks_all_pass=True` on every row of the mass–spring, benchmark and tree runs.

## Regenerated results against the versioned ones

| File | Verdict |
|---|---|
| `interval_certification/results/aggregate.csv` | identical |
| `interval_certification/results/aggregate_prec512.csv` | identical |
| `interval_certification/results/kappa_audit.csv` | identical |
| `interval_certification/results/kappa_audit_prec512.csv` | identical |
| `interval_certification/results/params.csv` | identical |
| `interval_certification/results/params_prec512.csv` | identical |
| `exact_benchmarks/results/aggregate.csv` | equal in every cell except wall-clock (`seconds`, and the seconds element of each `dps_trail` tuple; the precisions and values in `dps_trail` are identical) |
| `exact_benchmarks/results/masses.csv` | identical |
| `exact_benchmarks/results/sweep.csv` | identical |
| `exact_benchmarks/results/decay_fits.csv` | identical |
| `binary_tree_family/results/aggregate.csv` | identical |
| `binary_tree_family/results/fixed_pair.csv` | identical |
| `binary_tree_family/results/radial_constants.csv` | identical |
| `binary_tree_family/results/theoremC_choice.csv` | identical |
| `mode_localization/results/aggregate.csv` | identical |
| `mode_localization/results/participation.csv` | identical |

Other checks:
- **`figure_manifest.json`**: identical.
- **`itf_vs_bounds.pdf`**: regenerated. One page, 6.34 × 2.46 in, all fonts embedded. It is
  not versioned in this release.
- **`experiment_log.json`**: the rerun appended 77 runs (3 certification, 33 benchmark,
  33 tree, 8 mass–spring). All 77 have status `success`.
- **Logged hashes**: the SHA-256 values that the new certification runs logged for their six
  CSV outputs match the regenerated files, which are the versioned files.
- **`env/requirements.txt`**: rewritten by each run, as the README warns. In the fresh
  environment it lacks `sympy`, which appears in the recorded freeze but is not imported by
  any script.
