# Python 3.12 (results produced with 3.12.3). Exact versions used for experiments/;
# the full pip freeze of each run is in experiments/<id>/env/requirements.txt.
numpy==2.5.3
scipy==1.18.1
mpmath==1.3.0
python-flint==0.9.0
matplotlib==3.11.2
SciencePlots==2.2.2
